# Implement Feature

Write the actual feature implementation.

## Input

From previous step:
- Scaffolded files: $SCAFFOLDED_FILES

## Tasks

1. **Implement feature logic**
   - Fill in component functionality
   - Add hook logic
   - Implement state management

2. **Add validation**
   - Add Zod schemas for input validation
   - Handle errors gracefully

3. **Style with TailwindCSS**
   - Apply consistent styling
   - Handle dark/light themes

4. **Update types**
   - Ensure TypeScript types are complete
   - Export new types from lib/types.ts

## Output

```markdown
## Implementation Complete

Files implemented:
- `path/to/component.tsx` - Feature logic added
- `path/to/hook.ts` - Custom hook implemented

Type updates:
- `lib/types.ts` - Added BookmarkFilter interface

The feature is ready for testing.
```
